// Squelette du moteur de workflow (à implémenter)

pub struct WorkflowEngine;

impl WorkflowEngine {
    pub fn new() -> Self {
        Self
    }

    pub fn run(&mut self) {
        log::info!("Moteur démarré (placeholder)");
    }
}
